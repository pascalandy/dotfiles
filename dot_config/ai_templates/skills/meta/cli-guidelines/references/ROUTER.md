---
name: cli-guidelines
description: CLI design, implementation, and agent-operability auditing. USE WHEN design CLI, CLI spec, CLI parameters, CLI surface area, command tree, USAGE synopsis, args table, flags table, exit code map, config precedence, refactor CLI surface, CLI interface design, build CLI, implement CLI, create CLI, CLI implementation, CLI best practices, CLI help text, CLI errors, CLI output, CLI flags, CLI args, CLI subcommands, review CLI UX, CLI code review, audit CLI, agent-friendly CLI, CLI requirements, CLI composability, CLI pipeline, CLI idempotent, CLI retry safe, CLI review agent, CLI operability, CLI non-interactive, CLI safety check.
---

# cli-guidelines

## Routing

| Request Pattern | Route To |
|---|---|
| design CLI, CLI spec, CLI parameters, CLI surface area, command tree, args table, flags table, exit code map, config precedence, refactor CLI surface | `CliSpec/SKILL.md` |
| build CLI, implement CLI, create CLI, CLI best practices, CLI help text, CLI errors, CLI output, CLI flags, CLI args, CLI subcommands, review CLI UX, CLI code review | `CliImpl/SKILL.md` |
| audit CLI, agent-friendly CLI, CLI requirements, CLI composability, CLI pipeline, CLI idempotent, CLI retry safe, CLI operability, CLI non-interactive, CLI safety check | `CliAudit/SKILL.md` |
