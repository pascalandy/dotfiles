---
name: coding-standards
description: Language and framework coding standards -- CLI design/implementation/auditing, Bash scripting conventions, Python development with uv, Starlette/ASGI web framework patterns. USE WHEN design CLI, CLI spec, CLI parameters, CLI surface area, command tree, args table, flags table, exit code map, config precedence, build CLI, implement CLI, create CLI, CLI best practices, CLI help text, CLI errors, CLI output, review CLI UX, audit CLI, agent-friendly CLI, CLI composability, CLI idempotent, CLI retry safe, bash script, shell script, shellcheck, shfmt, strict mode, fct_ naming, create shell script, refactor shell script, lint shell script, debug shell error, python, uv, PEP 723, pyright, ruff, pytest, python script, python project, type hints, python environment, starlette, ASGI, FastAPI internals, routing, middleware, WebSocket, templates, static files, authentication, background tasks, ASGI server, uvicorn.
---

# coding-standards

## Routing

| Request Pattern | Route To |
|---|---|
| design CLI, CLI spec, CLI parameters, CLI surface area, command tree, args table, flags table, exit code map, config precedence, refactor CLI surface | `CliSpec/MetaSkill.md` |
| build CLI, implement CLI, create CLI, CLI best practices, CLI help text, CLI errors, CLI output, CLI flags, CLI args, CLI subcommands, review CLI UX, CLI code review | `CliImpl/MetaSkill.md` |
| audit CLI, agent-friendly CLI, CLI requirements, CLI composability, CLI pipeline, CLI idempotent, CLI retry safe, CLI operability, CLI non-interactive, CLI safety check | `CliAudit/MetaSkill.md` |
| bash script, shell script, shellcheck, shfmt, strict mode, fct_ naming, create shell script, refactor shell script, lint shell script, debug shell error | `Bash/MetaSkill.md` |
| python, uv, PEP 723, pyright, ruff, pytest, python script, python project, type hints, python environment, python development | `Python/MetaSkill.md` |
| starlette, ASGI, FastAPI internals, routing, middleware, WebSocket, templates, static files, authentication, background tasks, ASGI server, uvicorn | `Starlette/MetaSkill.md` |
